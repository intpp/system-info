System (Server) information - system-info
===========

Information about the server.

Usage
===========

```php
    $system = Server::getInfo();
    
    $system::getName();
    $system::getVersion();
    ...
    $system::getUptime();
```